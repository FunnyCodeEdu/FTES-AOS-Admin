# Tasks — admin-device-oversight-appeals

- [x] 1.1 `deviceOversight.api.ts`: hook danh sách/khoá/đơn/quyết định (coreClient, type trang riêng
      `IdentityPage` khớp `{items, page, size, totalElements}` của BE)
- [x] 1.2 `DeviceOversightPage`: bảng + chỉnh ngưỡng/cửa sổ + tag vi phạm + confirm khoá
- [x] 1.3 `UnlockAppealsPage`: hàng đợi + duyệt/từ chối kèm ghi chú
- [x] 1.4 Đăng ký 2 route + nav
- [x] 2.1 `tsc --noEmit` sạch, `npm test` xanh (500 test)
