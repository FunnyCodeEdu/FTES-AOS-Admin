# Tasks — admin-story-expiry-badge

- [x] 1.1 `STORY_VISIBLE_HOURS` + `storyVisibility()` trong `format.ts`
- [x] 1.2 `ClipStudioPanel`: LIVE → "Đang trên mục Tin", EXPIRED → "Đã hết 24h hiển thị"
- [x] 1.3 Thiếu `publishedAt` thì coi như LIVE (không dán nhãn sai lên tin đang chạy)
- [x] 2.1 `storyVisibility.test.ts`: chưa đăng / vừa đăng / quá hạn / thiếu mốc / đúng biên 24h
- [x] 2.2 `npm run build` + `npx vitest run` xanh
