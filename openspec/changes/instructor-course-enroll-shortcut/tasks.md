# Tasks — instructor-course-enroll-shortcut

- [x] 1.1 `MyCoursesPage`: nút "Thêm học viên" mỗi dòng, mở `GrantEnrollmentModal`
- [x] 1.2 Chuyển `TeachingCourse` → `Course` ở một chỗ duy nhất (modal chỉ đọc id/name/saleMode)
- [x] 1.3 Gate `admin.course.manage` — đúng quyền BE đang gác
- [x] 1.4 Dùng `ResponsiveTable` + `MobileCard` cho màn điện thoại
- [x] 1.5 `npm run build` xanh + `npx vitest run` xanh (699 test)
